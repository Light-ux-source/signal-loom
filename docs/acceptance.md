# 验收清单

## 网页功能

- [ ] 点击 `Scan now` 后显示扫描状态，并请求 `/api/scan`
- [ ] 点击左侧关键词可以筛选信息流
- [ ] 点击 `+` 可以添加新的监控关键词
- [ ] 搜索框可以按标题、摘要和作者过滤
- [ ] `Mark all read` 可以更新信号状态
- [ ] 没有密钥时页面仍可正常运行
- [ ] 375px、768px、桌面宽度下无横向溢出

## 服务功能

- [ ] Demo 模式返回结构化信号
- [ ] 配置 X Token 后调用 Recent Search
- [ ] 配置 OpenRouter Key 后返回 AI 评分
- [ ] 空关键词返回 400
- [ ] 上游失败返回 502

## 当前已执行

- `npm run lint`：通过
- `npm run build`：通过
